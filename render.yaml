services:
  - type: web
    name: fact-check
    env: python
    region: oregon
    plan: free
    buildCommand: 
      - apt-get update && apt-get install -y ffmpeg
      - pip install -r requirements.txt
    startCommand: gunicorn app:app
